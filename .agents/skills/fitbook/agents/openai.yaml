interface:
  display_name: "Fitbook"
  short_description: "Repo-specific patterns and workflows for fitbook"
  default_prompt: "Use the fitbook repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
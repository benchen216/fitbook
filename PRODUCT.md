# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

產品內有五種角色，每一種都有自己的畫面與作業流程：

- **會員**（如「王小明」）：查看自己的會籍狀態與到期日、瀏覽並預約課程、管理已預約與候補、查詢自己的缺席記點與停權狀態。目標是自助完成，不必打電話或到櫃檯問。
- **櫃檯人員**：在門市現場處理會員建檔、會籍購買／標記付款／凍結解凍、代客預約、以及開課前後的報到作業。作業範圍限定在自己所屬分店。
- **店經理**：管理本分店的課程、員工帳號（櫃檯人員／教練）與啟用狀態；同時具備櫃檯作業權限，可直接執行本分店的會員與會籍作業。
- **教練**：在綁定的分店與姓名下開課，管理自己的課程班表與學員出席。分店與教練姓名綁定帳號，開課時不可更改。
- **未登入訪客**：可瀏覽公開頁面，不保留任何資料。

此外有一類非產品使用者但決定專案成敗：**健身房決策者／客戶**，也就是這份 Demo 的觀看對象。

## Product Purpose

FitBook 是一套單一連鎖健身房的內部營運系統，把「會員自助預約」與「櫃檯／店經理／教練的現場作業」放進同一個系統，讓會籍狀態、課程名額、候補順位、缺席記點與報到結果彼此連動，而不是靠人工登記與口頭判斷。

目前這個 codebase 的定位是**可展示的 Demo／提案**，不是上線系統。成功的定義是：健身房決策者看完之後相信這是一套可以導入的商用系統，願意推進下一步；而不是分數、驗收或內部交付。

## Positioning

同類工具通常只做「線上訂課」，把會籍、爽約、候補、報到留給櫃檯人工處理。FitBook 的差異在於**把營運規則本身做成產品邏輯**：滾動窗口的缺席記點會自動導致停權並自動恢復、候補在名額釋出時自動遞補並通知、停課會直接標記所有預約為已取消且不記缺席點、凍結期間到期日不倒數且不可預約。這些規則在會員端與員工端看到的是同一份事實。

## Operating Context

- **多分店連鎖**：現有資料涵蓋信義店、大安店、板橋店。櫃檯與教練的作業範圍限定在自己所屬分店；報到頁只顯示本分店課程。
- **課程生命週期**：可預約 → 滿額可候補 → 候補已滿 → 已開始 → 已結束；另有已停課的終止狀態。
- **預約生命週期**：已確認 → 開課後依報到情況轉為已報到或未報到（記一次缺席點）；候補中可自動轉為已確認；停課時全部轉為已取消。
- **報到窗口**：開課前 30 分鐘至該堂課結束之間才能報到。
- **現場節奏**：櫃檯作業發生在會員站在櫃檯前的當下（快速建檔、查手機、標記付款、開課前的一批報到），時間壓力高。
- **展示情境**：這份 Demo 主要在筆電螢幕上向客戶展示。

## Capabilities and Constraints

**已確認的業務規則**

- 缺席（未報到）以最近 30 天為滾動窗口計算記點；累積達 3 點暫停預約權 7 天，到期自動恢復；滾出窗口的記點不再計入門檻。
- 停權期間仍可取消已預約課程。
- 會籍狀態：無會籍／待付款／生效中／凍結中／已到期。
- 凍結次數上限為 2 次；凍結期間到期日不倒數，且無法預約課程。
- 課程有名額上限與候補上限兩個獨立數字。
- 教練的分店與姓名綁定帳號，開課時不可更改。
- 員工帳號角色為櫃檯人員或教練，可停用。

**現有實作範圍**

- 14 個畫面／路由（登入角色選擇 + 13 個角色畫面），路由表在 `src/App.jsx`。
- 技術棧為 Vite + React 18 + React Router 6；樣式為 `src/styles/` 下的 CSS 代幣與元件層，搭配 `src/components/ui.jsx` 共用元件與 `src/components/icons.jsx` 圖示系統。視覺系統記錄在 `DESIGN.md`。
- `prototypes/` 內有 14 份原始 HTML 原型，是重新設計前的畫面來源參考，不再代表現行外觀。

**明確的缺口（Demo 階段可接受，不可假裝已有）**

- 沒有認證，沒有權限守衛，`App.jsx` 的路由僅供示意。
- 沒有資料層，所有動作接在 `useState` 上。
- 頁面上的「示範」切換控制項（凍結／生效、有預約／無預約等）是為了在同一個檔案內展示所有視覺狀態而存在的，**不是真實產品控制項**，接真實資料後應移除。
- 沒有付款串接、沒有通知寄送（候補遞補「會通知你」目前只是文案）。

**未決事項**

- 是否需要 i18n 尚未決定；目前介面全為繁體中文（台灣）。
- 沒有既有的行動版設計；會員端在真實情境會用手機，但本次展示以筆電為準。

## Brand Commitments

**目前沒有任何綁定的品牌或視覺承諾。** 使用者明確表示現有的一切都可以更動，目標是讓成品看起來像一套商用產品。

- 「FitBook」是現行名稱，非鎖定。
- 原本的 Broadsheet 設計系統（cyan/magenta 色票、Source Serif 4、4px 圓角）已於重新設計時整套汰換，不再是約束或參考。
- 現有的業務規則文案與角色名稱屬於產品事實，即使視覺全面重做也須保留其語意。

## Evidence on Hand

- **有的**：14 份互動 HTML 原型（`prototypes/`）、14 個 React 畫面（`src/pages/`）、既有 token 檔（`src/tokens.js`）、README 內的設計交付說明。畫面內的分店名、課名、教練名、會員名皆為示範資料。
- **沒有的，且不得捏造**：真實客戶、推薦語、導入案例、營運數據、成效數字、定價方案、媒體報導、獲獎紀錄、上線時程。
- **沒有素材**：全站沒有圖示系統與攝影素材（訪客空狀態上有一個 inline 鎖頭 SVG）。任何需要影像或圖示的方向都必須自行產出或明確標示為待補。

## Product Principles

1. **規則要在畫面上說清楚，而不是事後才罰。** 記點門檻、停權期限、凍結額度、報到窗口都應在使用者做決定之前就看得到。
2. **同一份事實，五種視角。** 會員看到的名額、候補序號與狀態，必須與櫃檯、教練看到的是同一件事，不能有兩套說法。
3. **櫃檯的每一秒都是排隊時間。** 現場作業以最少步驟完成為準；查詢、建檔、標記付款、報到都要能在會員還站在櫃檯時做完。
4. **狀態不是裝飾，是產品的主體。** 這套系統的價值幾乎全部在狀態流轉上，畫面必須讓當下狀態與下一步動作一眼可辨。
5. **這是要賣出去的東西。** 每個畫面都會被決策者當成「導入後的樣子」來評估，完成度本身就是說服力。
